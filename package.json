{
  "name": "paperclips",
  "version": "1.0.0",
  "description": "",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "parcel build src/index.html --public-url=/paperclips/dist/ --detailed-report",
    "serve": "live-server --port=1234 ./live/",
    "watch": "parcel watch ./src/index.html --no-hmr --dist-dir ./live/",
    "start": "npm-run-all --parallel watch serve"
  },
  "author": "Robert Leitl",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.0.0",
    "live-server": "^1.1.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@tweakpane/plugin-essentials": "^0.1.4",
    "gl-matrix": "^3.4.3",
    "tone": "^14.7.77",
    "tweakpane": "^3.0.6"
  }
}
